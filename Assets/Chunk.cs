using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chunk : MonoBehaviour
{
    //[SerializeField] private int length;
    void Start()
    {
        //End = (End==null?new Transform(0,30,0):End)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
